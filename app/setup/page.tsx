"use client";
import { FormEvent, useState } from "react";
export default function Setup(){const [playerId,setId]=useState("kouta"),[displayName,setName]=useState("コウタ"),[password,setPass]=useState(""),[setupCode,setCode]=useState(""),[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg("作成中…");const r=await fetch("/api/auth/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({playerId,displayName,password,setupCode})});const d=await r.json();if(r.ok)location.href="/login";else setMsg(d.error)}return <main className="login"><form onSubmit={submit}><div className="logo">▲</div><h1>初期管理者の作成</h1><p>この画面は最初の1回だけ使えます。</p><label>管理者ID<input value={playerId} onChange={e=>setId(e.target.value)} required/></label><label>表示名<input value={displayName} onChange={e=>setName(e.target.value)} required/></label><label>パスワード（8文字以上）<input type="password" minLength={8} value={password} onChange={e=>setPass(e.target.value)} required/></label><label>セットアップコード<input type="password" value={setupCode} onChange={e=>setCode(e.target.value)} required/></label>{msg&&<div className="error">{msg}</div>}<button>管理者を作成</button></form></main>}
